## 本次变更

- 国内加速地址支持自定义、保存时连通性校验及恢复默认。
- 支持分组独立负载算法和主机粘滞故障切换保持期，修复跨分组轮询公平性及最小连接数平局偏置。
- 完善 HTTP 入站分片识别、连接取消回收和双向传输统计；优化 DNS 并发刷新、日志翻页及统计缓存。
- 更新包改为流式下载，校验发布者签名、内容摘要及版本和平台绑定，拒绝篡改或不匹配的产物。
- 修复 Windows 便携更新导致应用与数据根目录漂移的问题，保持原入口并支持启动失败回滚。
- 发布流程支持明确的签名自动安装与手动分发模式；缺密钥或缺清单时提供官方手动更新入口，验签失败仍拒绝安装。
- macOS 使用无需开发者证书的 ad-hoc 签名，保留系统安全提示与 HTTPS 校验。
- 修复 HTTP 转发仅 TCP 连接成功就提前恢复健康的问题；认证失败可连续累计，半开试探等待有效响应，HTTP 5xx 不记为业务成功。
- 单条本地网络路径不可达时，在原分组、尝试次数与总超时内继续切换候选，不误伤代理健康状态。
- 拨号容量满时重新按策略选择可用节点，轮询与自适应学习游标只在实际预占成功后推进。
- 健康状态即时生效，并按节点独立合并、重试后台持久化；日志压力不再阻止节点恢复，新增状态队列诊断计数。
- 选路评分保留微秒精度，修复亚毫秒节点被当作未知耗时而评分偏低的问题。
- 减少选路中的重复筛选和无效克隆；清空日志使用有序写入屏障，清空前的旧日志不会在之后重新写回。
- 流量日志默认每页 10 条，分页选项调整为 10、20、30、40、50 条。
- 测活改为固定节点的单链路探测，区分代理认证、目标链路和本地资源失败；展示分阶段诊断，避免目标站点故障误伤代理健康。
- 合并同节点重叠测活，限制排队等待并支持取消；配置变更后的旧结果不再覆盖新状态。
- 为代理分组成员和域名规则建立索引，减少选路中的重复筛选、配置复制与持锁时间。
- 新增可选的目标链路质量观测与自适应评分，默认关闭，按目标隔离样本并限制缓存容量。
- 高级设置支持调整业务连接及拨号并发上限，区分已保存与当前生效值，重启应用后生效。
- 高级设置改为左右高度平衡的瀑布流卡片布局，窄窗口自动切换为单列。
- 修复测活 HTTP 响应头长度限制在不同读取分片下失效的问题；累计限制临时响应和最终响应头，不将响应正文计入上限。
